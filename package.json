{
  "name": "fin-vision",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/auth-profile.test.js",
    "test:browser": "npm run build && playwright test",
    "amplify:sandbox": "ampx sandbox"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.24.0",
    "@aws-amplify/backend-cli": "^1.9.0",
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "aws-amplify": "^6.20.0"
  }
}
